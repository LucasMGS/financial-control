import 'package:flutter/material.dart';

class CreateBankAccountPage extends StatelessWidget {
  const CreateBankAccountPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Title'),
      ),
      body: Container(),
    );
  }
}
